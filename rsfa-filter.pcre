# do not filter any mails that already ran through RSFA
/^X-RSFA-Orig-Subject: .*/                        DUNNO
# Match all Subjects - required for encoded subject headers, where we cannot explicitly match on subject tags
/^Subject: .*/                                    FILTER xof-filter:
